# URL of the backend API — must be set at build time (Next.js bakes NEXT_PUBLIC_* vars in at build)
# For local dev this defaults to http://localhost:8000 and can be omitted
# For production, set this to the deployed backend URL before running `next build`
NEXT_PUBLIC_API_URL=https://your-backend.onrender.com
